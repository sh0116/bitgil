# Inference-layer glue (GPLv2).
# Selects the configured eyemate_core provider adapter and drives the
# SessionContext -> provider -> post-process flow. Thin wrapper over core.
# TODO(M1): construct provider from NVDA config; wire streaming to output layer.
