# 시연 영상 대본 (3분 이내)

대회 제출용. 실측 시간까지 넣어 두었으니 **총 2분 50초** 안에 들어갑니다. 각 컷의
"말할 것"은 그대로 읽으면 되는 문장이고, "할 것"은 화면에서 할 조작, "칠 것"은 그대로 복사해
붙이는 명령입니다.

## 녹화 전 준비 (10분)

주 시연 화면은 **브라우저**입니다(`http://localhost:8765/tutor.html`). 터미널 REPL은 같은
코어를 두드리는 개발용 경로여서, 사용 장면으로는 쓰지 않습니다.

```bash
cd ~/Documents && source .venv/bin/activate
export AWS_REGION=ap-northeast-2          # Bedrock 호출용 (키는 ~/.aws)
python -m pytest -q                        # 279 passed 확인 (컷 6에서 보여줍니다)
python web/server.py --provider bedrock --profile learning-chart
#  → 브라우저에서 http://localhost:8765/tutor.html
```

### ⚠️ 먼저 60초 예행 (이것부터 하세요)

**이 화면은 파이에서 브라우저로 열어 본 적이 없습니다.** 파이에 headless 브라우저가 없어
(chromium·firefox 둘 다 이 환경에서 기동 실패) HTTP 계약과 DOM 배선만 자동 검증했습니다.
녹화 전에 맥에서 다음을 눈으로 확인하세요. 하나라도 어긋나면 **컷 2~4를 §부록의 터미널
버전으로 대체**하고(내레이션은 그대로 쓸 수 있습니다) 그 사실을 보고서에 적지 마세요 —
영상만 바꾸면 됩니다.

1. 페이지가 뜨고 맨 아래에 `프로바이더: bedrock · 도표 설명 프로파일: learning-chart`가 보인다.
2. 시험지를 올리면 개요가 뜨고 **소리가 실제로 난다**("시험지 원문. 2026학년도…").
3. `2번` 칩을 누르면 즉시(0.0초) 원문이 뜨고 태그가 초록 `원문`이다.
4. `도표 설명` 버튼이 10초 안에 파란 `모델` 응답을 준다.
5. `복습 노트 저장`이 `bitgil-review.md`를 내려받고 저장했다고 **말한다**.

### 그 밖의 준비

- 브라우저 창을 화면 **오른쪽 절반**, `docs/demo/모의고사_샘플.pdf`를 PDF 뷰어로 **왼쪽 절반**에
  둡니다. 심사위원이 "시험지에 뭐가 인쇄돼 있는지"를 눈으로 확인할 수 있어야 우리 주장이
  검증 가능해집니다.
- 브라우저 글꼴/확대를 **125~150%**로. 화면 녹화에서 작은 글씨는 안 읽힙니다.
- 첫 Bedrock 호출은 연결 수립 때문에 느립니다. **녹화 전에 도표 설명을 한 번 돌려** 두면
  본 녹화에서 10초 안에 나옵니다.
- **소리 겹침 주의.** 이 화면은 스스로 한국어로 낭독합니다. 컷 2의 첫 응답에서 **5초 정도
  실제 낭독 소리를 그대로 들려주고**(접근성 도구 시연에서 가장 강한 증거입니다), 그 뒤에는
  `음성 낭독 켜기` 체크를 **끄고** 내레이션을 얹으세요. 켠 채로 내레이션하면 둘이 겹칩니다.
- 음성: 아래 내레이션을 직접 읽어 넣습니다. 접근성 도구 시연이므로 **자막(캡션)도 필수**입니다.

---

## 컷 1 · 문제 제기 (0:00 – 0:22, 22초)

**화면**: 시험지 PDF의 2쪽(막대그래프 문항)을 크게. 그래프 위에 커서를 천천히 움직입니다.

**말할 것**
> 시각장애 학생의 스크린리더는 이 문장은 읽습니다. 하지만 이 그래프는 읽지 못합니다.
> 그래서 비전 AI를 쓰는데, 선행 연구에서는 자동 생성 차트 설명의 대다수에 사실 오류가
> 있고 그중 가장 많은 것이 값 오류라고 보고합니다. 더 심각한 건, 시각장애 사용자가 그
> 오류를 절반 수준밖에 탐지하지 못한다는 보고입니다. 그래서 빛길은 "더 잘 설명하는 도구"가
> 아니라 "틀렸을 때 틀렸다고 말하는 도구"로 만들었습니다.

> **주의**: 이 컷에서 "81퍼센트" 같은 구체 수치를 단정해 말하지 마세요.
> [../evidence.md](../evidence.md)가 그 수치들을 **초록 수준(재확인 필요)**으로 표시해 두었고,
> 검증 안 된 숫자를 말하는 것은 이 프로젝트가 비판하는 바로 그 행동입니다. 위 문장처럼
> "대다수", "절반 수준"으로 말하면 정확하면서도 충분히 강합니다.

---

## 컷 2 · 시험지를 펼치면 무슨 시험지인지 말하고 기다린다 (0:22 – 0:52, 30초)

**할 것**: 브라우저의 `1. 시험지 열기`에 `모의고사_샘플.pdf`를 **끌어다 놓습니다.** 그다음
`2번` 칩을 누릅니다.

**화면**: 개요 말풍선(초록 `원문` 태그) → 문항 번호 칩이 생기는 것 → `2번`을 누르면 `0.0초`와
함께 지문·선택지가 즉시 뜨는 것. PDF 뷰어의 같은 문항을 나란히 놓아 **글자가 정확히 일치**하는
것을 보여줍니다.

**여기서 낭독 소리를 그대로 5초 들려주세요.** 내레이션은 그 뒤에 얹습니다.

**말할 것**
> 파일을 올리면 곧바로 해설을 시작하지 않습니다. 무슨 시험지인지, 몇 문항인지, 어느 문항에
> 도표가 붙어 있는지 먼저 말하고 **기다립니다.** 파일 이름이 아니라 시험지에 인쇄된 머리글을
> 읽은 것이고, 이 응답까지 AI 호출은 0회입니다.

> 지문과 선택지도 AI에게 묻지 않습니다. PDF의 텍스트 레이어에서 그대로 읽습니다. 0.0초입니다.
> 환각이 적은 게 아니라, 환각이 생길 자리가 아예 없습니다. 초록색 "원문" 표시가 그 뜻이고,
> 음성으로도 "시험지 원문"이라고 먼저 말합니다 — 화면을 못 보는 사용자에게는 그 한마디가
> 유일한 구분입니다.

> 도표가 있으면 그 사실만 알려주고, 축 눈금 숫자는 읽지 않습니다. 소리로 들으면
> "옳은 것은? 0 65 130 195"가 되어 소음이 되기 때문입니다. 대신 그 숫자들은
> 뒤에서 대조용 근거로 남겨 둡니다.

---

## 컷 3 · 도표만 비전 모델로 (0:52 – 1:28, 36초)

**할 것**: `도표 설명` 버튼을 누릅니다.

**화면**: 파란 `모델` 태그 + 경과 시간(실측 9.7초). 값 목록(1월 120 / 2월 200 / 3월 90 /
4월 260)이 나오면 PDF 뷰어의 막대 라벨을 하나씩 커서로 짚습니다.

**말할 것**
> 도표는 텍스트 레이어에 값이 없으니 이때만 비전 모델을 씁니다. 필요한 페이지 한 장만
> 이미지로 만들어 보냅니다. 응답에 "모델"이라고 표시되죠. 원문과 모델의 말이 같은 목소리로
> 읽히기 때문에, 무엇을 믿을지는 사용자가 정할 수 있어야 합니다.

> 인쇄된 네 값이 모두 정확합니다. 하지만 정확했다는 걸 우리가 어떻게 알까요. 다음 문항이
> 그 질문에 답합니다.

---

## 컷 4 · 가드레일 ① 보간을 거부한다 (1:28 – 2:05, 37초)

**할 것**: `3번` 칩 → `도표 설명` 버튼.

**화면**: PDF 뷰어를 2쪽으로. **2023년 막대에 값 라벨이 없는 것**을 커서로 짚어 보여줍니다.
그리고 모델 응답 중 "2023년은 수치가 표시되어 있지 않습니다" 줄을 확대합니다(2026-08-27
Bedrock 실측 문장입니다).

**말할 것**
> 이 문항은 일부러 이렇게 만들었습니다. 2023년 막대에는 값 라벨이 없습니다. 눈금으로 보면
> 500 근처라고 말하고 싶어지죠. 차트 설명 오류 1위가 정확히 이 보간입니다.

> 모델은 보간하지 않았습니다. "라벨이 없어 읽을 수 없습니다"라고 답했습니다. 그리고
> 값을 물어봐도 정답을 대신 고르지 않습니다. 정답을 알려주면 학습을 대신해 버리니까,
> 근거가 되는 원문을 가리키는 데까지만 답합니다.

---

## 컷 5 · 가드레일 ② 프롬프트를 믿지 않는 두 번째 층 (2:05 – 2:32, 27초)

**말할 것 (먼저)**
> 그런데 방금 그건 모델이 지시를 잘 따라준 결과입니다. 프롬프트는 부탁이고, 언제든
> 안 지켜질 수 있습니다. 그래서 나가는 문장을 코드로 한 번 더 검사합니다.

**칠 것** (새 터미널에서. 모델이 값을 지어냈다고 가정한 입력을 직접 넣습니다)
```bash
python -c "
import sys; sys.path.insert(0, 'core')
from bitgil_core.document import load_pdf
from bitgil_core.factcheck import annotate_unsupported
doc = load_pdf('docs/demo/모의고사_샘플.pdf')
근거 = [doc.question(3).source_text(), doc.page_text(1)]
for 해설 in ['2023년 막대는 눈금으로 볼 때 약 480명입니다.',
             '2023년 이용자 수는 520명입니다.']:
    print(annotate_unsupported(해설, 근거)[0], '\n')
"
```

**화면**: 두 줄 모두에 `확인 필요: … 원문에서 확인되지 않은 숫자입니다.` 고지가 붙는 것.

**말할 것**
> 지어낸 480도 걸리고, 사실은 정답인 520도 걸립니다. 520은 그래프에 인쇄돼 있지 않으니까요.
> 기준은 "맞았는지"가 아니라 "화면에 인쇄돼 있는지"입니다. 그리고 이 검사는 모델의 문장을
> 지우지 않고 고지만 덧붙입니다. 지우면 뜻이 바뀌고, 무엇이 사라졌는지도 알 수 없기
> 때문입니다. 프롬프트가 아니라 규칙이라서, 모델을 바꿔도 그대로 작동합니다.

---

## 컷 6 · 오픈소스로서의 마무리 (2:32 – 2:50, 18초)

**칠 것**
```bash
python -m pytest -q && ruff check core/ tests/ scripts/
```

**화면**: `279 passed` → `All checks passed!`. 이어서 GitHub 저장소 페이지를 잠깐 보여줍니다.

**말할 것**
> 테스트 279건 전부 오프라인으로 돌아갑니다. API 키가 없어도 검증할 수 있습니다.
> 재사용 코어는 MIT, NVDA 애드온은 GPL 버전 2, 프로파일은 CC BY 4.0으로 분리했습니다.
> PDF 처리에 AGPL 라이브러리를 쓰지 않은 것도 그 때문입니다. 화면을 못 보는 사람이
> AI의 말을 검증할 방법을 만드는 것, 빛길이 하려는 일입니다.

---

## 넣지 말 것

- **실 기출문제**: 저작권. `docs/demo/모의고사_샘플.pdf`는 같은 형식으로 직접 만든 자작 문항입니다.
- **API 키·자격증명이 보이는 화면**: `~/.aws`, `.env`, `env | grep` 같은 화면을 절대 담지 마세요.
  터미널 스크롤백에 이전 세션의 키가 남아 있을 수 있으니 **새 터미널에서 녹화**하세요.
- **없는 기능 주장**: NVDA 실기기 낭독 품질은 아직 검증하지 않았습니다. 영상에서 "NVDA에서
  이렇게 들립니다"라고 말하지 마세요. 웹 클라이언트로 시연하는 것은 사실이고, 그렇게 말하면 됩니다.

## 여유가 있으면 (선택 컷, 15초)

같은 서버의 `index.html` → **화면 공유 해설 모드**. 시험지 PDF가 없는 화면(강의 슬라이드,
웹페이지)에는 이쪽을 씁니다. "입력이 문서든 화면이든 같은 코어를 씁니다" 한 문장으로 붙이면
플랫폼 무관 설계가 한 컷에 들어갑니다. 화면 공유는 **localhost 또는 HTTPS에서만** 되니
녹화도 localhost에서 하세요.

---

## 부록 · 컷 2~4를 터미널로 대체해야 할 때

브라우저 예행이 어긋났을 때만 씁니다. 같은 코어·같은 라우팅 규칙이라 내레이션은 그대로
쓰되, "초록색/파란색 태그" 대신 "대괄호 안의 원문·모델 표시"로만 바꿔 읽으세요.

```bash
python scripts/bitgil_tutor.py --pdf docs/demo/모의고사_샘플.pdf \
  --provider bedrock --profile learning-chart
```
프롬프트에 차례로: `2번 읽어줘` → `도표 설명해줘` → `3번 읽어줘` → `도표 설명해줘`.
화면에는 `[원문 0.0초]` / `[모델 9.7초]` 태그가 나옵니다. 터미널 글꼴은 16pt 이상으로.
