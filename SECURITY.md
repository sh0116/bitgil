# 보안 정책 / Security Policy

## 취약점 신고 / Reporting a vulnerability
보안 문제를 발견하면 공개 이슈로 올리지 말고, 메인테이너에게 비공개로 연락해 주세요
(GitHub Security Advisories 또는 저장소에 명시된 연락처). 확인 후 수정 및 공개 일정을
협의합니다.

## 설계상의 보안·프라이버시 원칙
Bitgil는 시각장애인의 화면(게임·학습 자료 등 개인정보 포함 가능)과 API 키를 다루므로
다음을 지킵니다.

- **BYO API Key** — 키는 사용자 소유. 저장소에 커밋 금지(`.gitignore`로 차단),
  로그·에러 메시지에 노출 금지.
- **화면 캡처 최소 전송** — 로컬 처리 우선, 외부(LLM 프로바이더) 전송은 명시적 동의 기반.
  프로바이더 어댑터는 이미지 바이트를 로깅·영속화하지 않는다.
- **로컬 모델 옵션** — Ollama 등 로컬 프로바이더로 화면을 외부로 보내지 않는 경로 제공.
- **캡처·세션 데이터 비추적** — 런타임 캡처/세션 로그/복습 노트는 화면 내용을 담을 수
  있으므로 `.gitignore`로 차단하고 사용자 로컬에만 보관.
- **게임 EULA 준수** — 게임 파일 미수정, 화면 캡처만 사용.

## CI 라이선스·시크릿 검사
CI에 라이선스 검사와 시크릿 스캔을 포함해 키 유출과 라이선스 충돌을 자동 검출한다.
